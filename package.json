{
  "name": "employee-app-fullstack",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start:frontend\" \"npm run start:backend\"",
    "start:frontend": "cd employee-data && npm start",
    "start:backend": "cd backend-firebase && node server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.1",
    "firebase": "^10.14.1",
    "firebase-admin": "^12.6.0"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  },
  "proxy": "http://localhost:5000"
}
